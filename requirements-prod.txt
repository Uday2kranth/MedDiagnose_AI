# ── Core Backend ──
fastapi
uvicorn[standard]
python-multipart

# ── Data & ML ──
pandas
numpy
scikit-learn
xgboost
shap

# ── LangChain & LLM Providers ──
langchain
langchain-google-genai
langchain-openai
langchain-ollama
langchain-mistralai

# ── Google Cloud Gmail API (for email tool) ──
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# ── Utilities ──
python-dotenv
requests

# ── Report Export ──
fpdf2
python-docx
matplotlib

# NOTE: Streamlit packages (streamlit, streamlit-shap, etc.) are intentionally excluded.
# They are only used by the legacy app.py (Streamlit UI) and are not needed for the
# FastAPI production backend. Add them back only if deploying the Streamlit app.
